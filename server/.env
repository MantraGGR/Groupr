# Copy to .env and fill values
PORT=8080
# The API key (or service account token) used to call Google Generative API from the server
GENERATIVE_API_KEY=your_generative_api_key_here

# A simple allowlist of emails (comma separated) for demo purposes
ALLOWLIST=alice@example.com,bob@example.com
